<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yangshengzhou.lucky_sms.mapper.student.StudentMapper">
    <!-- 通过uid返回学生信息 -->
    <select id="selectStudentById" parameterType="Integer" resultType="com.yangshengzhou.lucky_sms.vo.student.StudentVO">
        SELECT
            u.username
        FROM
            users u
                INNER JOIN students s ON u.user_id = s.user_id  -- 显式指定连接条件，更清晰
                INNER JOIN course_selections c ON s.student_id = c.student_id
        WHERE
            u.user_id = #{userId}
        GROUP BY
            u.username, s.student_no, s.gpa, s.class_rank  -- 非聚合字段需分组
    </select>
</mapper>